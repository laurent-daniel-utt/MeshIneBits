package bitSlicer;

import bitSlicer.util.Shape2D;
import bitSlicer.util.Vector2;

public class Bit2D extends Shape2D {
	
	/*
	 * originBit and orientation are in the coordinate system of the associated pattern
	 * everything but these two vectors are in the local coordinate system with the origin at the center of the bit 
	 */
	public Bit2D(Vector2 originBit, Vector2 orientation, double bitWidth, double bitLength){
		
	}

}
