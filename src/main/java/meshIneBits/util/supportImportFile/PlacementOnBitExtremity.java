package meshIneBits.util.supportImportFile;

/**
 * placement of the point on the bit (on what edge this point is located) we see that there is no Right placement because it's
 * the holding zone.
 * see Majed_Documents>Bit_Placements.png for more understanding
 */
public enum PlacementOnBitExtremity {
    Top,Bottom,Left,Topcorner,Bottomcorner;
}
